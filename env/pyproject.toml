[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["QuarterCar_env"]

[project]
name = "QuarterCar_env"
version = "0.0.1"
dependencies = [
  "gymnasium",
  "numpy",
  "scipy",
  "matplotlib",
  "Pillow",
]
